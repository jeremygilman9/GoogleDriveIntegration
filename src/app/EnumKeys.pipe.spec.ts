/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { EnumKeysPipe } from './EnumKeys.pipe';

describe('Pipe: Keyse', () => {
  it('create an instance', () => {
    let pipe = new EnumKeysPipe();
    expect(pipe).toBeTruthy();
  });
});